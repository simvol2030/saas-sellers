---
/**
 * Admin Blocks List
 *
 * CRUD управление повторяемыми блоками
 */
import Admin from '../../../layouts/Admin.astro';
import BlocksList from '../../../components/admin/BlocksList.svelte';

export const prerender = false;
---

<Admin title="Блоки">
  <script is:inline>
    (function() {
      if (!localStorage.getItem('accessToken')) {
        window.location.href = '/admin/login';
      }
    })();
  </script>

  <BlocksList client:load />
</Admin>
